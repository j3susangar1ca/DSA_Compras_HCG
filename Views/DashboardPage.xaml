<Page
    x:Class="DSA_Compras_HCG.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    Background="Transparent">

    <Grid Padding="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="20" Margin="0,0,0,20">
            <TextBlock Text="Dashboard de Folios DSA" Style="{StaticResource TitleTextBlockStyle}" />

            <Grid ColumnSpacing="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="20" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="En Registro Inicial" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock Text="{x:Bind ViewModel.TotalRegistroInicial, Mode=OneWay}" Style="{StaticResource TitleTextBlockStyle}"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="20" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Pendientes de Aval / Negativa" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock Text="{x:Bind ViewModel.TotalPendientesValidacion, Mode=OneWay}" Style="{StaticResource TitleTextBlockStyle}"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="20" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="En Cotización (Pilar 5)" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock Text="{x:Bind ViewModel.TotalEnCotizacion, Mode=OneWay}" Style="{StaticResource TitleTextBlockStyle}"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="3" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="20" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Completados / Pagados" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock Text="{x:Bind ViewModel.TotalPagados, Mode=OneWay}" Style="{StaticResource TitleTextBlockStyle}" Foreground="Green"/>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="+ Nuevo Pedido Directo" Style="{StaticResource AccentButtonStyle}" Command="{x:Bind ViewModel.IrANuevoPedidoCommand}"/>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="10">
                <ComboBox PlaceholderText="Filtrar por Estatus DFA" ItemsSource="{x:Bind ViewModel.ListaEstatusDFA}" SelectedItem="{x:Bind ViewModel.FiltroEstatus, Mode=TwoWay}" Width="200"/>
                <AutoSuggestBox PlaceholderText="Buscar Folio o Servicio..." QueryIcon="Find" Width="250" Text="{x:Bind ViewModel.TextoBusqueda, Mode=TwoWay}"/>
            </StackPanel>
        </Grid>

        <controls:DataGrid Grid.Row="2" 
                           ItemsSource="{x:Bind ViewModel.FoliosFiltrados, Mode=OneWay}"
                           AutoGenerateColumns="False"
                           CanUserSortColumns="True"
                           AlternatingRowBackground="{ThemeResource LayerFillColorDefaultBrush}"
                           GridLinesVisibility="Horizontal"
                           IsReadOnly="True">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Folio DSA" Binding="{Binding FolioDSA}" Tag="Folio"/>
                <controls:DataGridTextColumn Header="Fecha Recepción" Binding="{Binding FechaRecepcion, StringFormat='{}{0:dd/MM/yyyy}'}" />
                <controls:DataGridTextColumn Header="Servicio Solicitante" Binding="{Binding ServicioSolicitante}" Width="*" />
                <controls:DataGridTextColumn Header="Trámite" Binding="{Binding TipoTramite}" />

                <controls:DataGridTextColumn Header="Estatus DFA" Binding="{Binding EstatusDSA}" />

                <controls:DataGridTextColumn Header="Total c/IVA" Binding="{Binding TotalConIva, StringFormat='{}{0:C2}'}" />

                <controls:DataGridTemplateColumn Header="Acción">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Gestionar" Command="{Binding DataContext.GestionarFolioCommand, ElementName=RootLayout}" CommandParameter="{Binding FolioDSA}" Background="Transparent" BorderThickness="0" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</Page>