<Page
    x:Class="DSA_Compras_HCG.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    Background="Transparent">

	<Grid Padding="30">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0" Text="Dashboard DSA" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,20"/>

		<controls:DataGrid Grid.Row="2"
                           ItemsSource="{x:Bind ViewModel.FoliosFiltrados, Mode=OneWay}"
                           AutoGenerateColumns="False"
                           IsReadOnly="True"
                           GridLinesVisibility="Horizontal">
			<controls:DataGrid.Columns>
				<controls:DataGridTextColumn Header="Folio" Binding="{Binding FolioDSA}" />
				<controls:DataGridTextColumn Header="Fecha" Binding="{Binding FechaFormateada}" />
				<controls:DataGridTextColumn Header="Servicio" Binding="{Binding ServicioSolicitante}" Width="*" />
				<controls:DataGridTextColumn Header="Estatus" Binding="{Binding EstatusDSA}" />
				<controls:DataGridTextColumn Header="Total" Binding="{Binding TotalFormateado}" />
			</controls:DataGrid.Columns>
		</controls:DataGrid>
	</Grid>
</Page>